# Campus Logistics — Environment Config
# Copy this file to .env and fill in your values

# Server
PORT=3000
NODE_ENV=production

# Session secret — change this to a long random string!
SESSION_SECRET=change-me-to-a-long-random-secret-string-before-deploying

# Database file path (relative to project root)
DB_PATH=./campus-logistics.db
